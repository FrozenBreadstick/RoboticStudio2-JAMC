from launch import LaunchDescription
from launch_ros.actions import Node

def generate_launch_description():

    ld = LaunchDescription()

    #Need to implement steps of UR_README.md in this launch file to reduce manual intervention
    #Might need to make new classes for launch?

    return ld

